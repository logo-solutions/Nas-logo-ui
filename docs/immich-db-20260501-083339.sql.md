# NAS-logo-UI — Product Specification

## Overview

Un **hub d'authentification unifié et mobile-first** qui agrège **Paperless-ngx** (documents) et **Immich** (photos), avec SSO Tailscale unique, search unifiée, et deep linking intelligent vers les UIs natives de chaque service.

**Philosophie:** Ne pas réinventer les roues. Garder les UIs natives matures (Immich, Paperless), mais les unifier sous un SSO unique + ajouter la couche métier (search, ACL, orchestration, audit).

**Tech Stack:**
- Frontend: Next.js (React) — Mobile-first, responsive
- Backend: Python FastAPI — Token manager, proxy auth, orchestration
- Search: Meilisearch — Index unifié (photos + documents)
- Workflow: n8n (OCR Whisper, conversions, archivage)
- Infrastructure: Tailscale VPN, authentification via headers Tailscale
- Token Storage: JWT + Redis session store

---

## Services / API locaux (NAS)

| Service | URL locale | Type |
|---|---|---|
| Immich | http://100.113.214.55:2283/api | OpenAPI / Swagger UI |
| Paperless-ngx | http://100.113.214.55:8010/api/ | DRF browsable API |
| Paperless schema | http://100.113.214.55:8010/api/schema/ | OpenAPI YAML |
| Paperless docs | http://100.113.214.55:8010/api/docs/ | Redoc |
| n8n | http://100.113.214.55:5679/api/v1/docs | Swagger UI |
| Meilisearch | http://100.113.214.55:7700 | REST classique |

---

## User Stories & Features

### 1.1 Authentification & Hub Centralisé
- **US-1.1** SSO Tailscale unifié — login unique, tokens auto-gérés par FastAPI
- **US-1.2** Dashboard d'accueil — stats globales, liens rapides, activité récente

### 1.2 Recherche & Découverte
- **US-2.1** Recherche full-text unifiée (Meilisearch) — résultats mixtes docs + photos
- **US-2.2** Filtrage avancé — type, date range, tags, source

### 1.3 Deep Linking & Navigation
- **US-3.1** Navigation transparente vers UIs natives (Immich/Paperless) avec auto-login
- **US-3.2** iOS & Android support — PWA optional

### 1.4 Gestion des Droits & Audit
- **US-4.1** ACL centralisée — permissions par utilisateur/service
- **US-4.2** Partage temporaire — liens avec expiration

### 1.5 Orchestration & Workflows
- **US-5.1** Archivage progressif via n8n
- **US-5.2** OCR & Enrichissement (Paperless natif + Whisper via n8n)
- **US-5.3** Backup vers Hetzner Storage Box (n8n)

### 1.6 Monitoring & Audit
- **US-6.1** Logs d'accès centralisés (user, timestamp, IP Tailscale, action)
- **US-6.2** Monitoring Grafana + alertes ntfy

---

## Phases de développement

### Phase 1 — MVP (Semaines 1-3)
- [ ] SSO Tailscale : login unique, token manager FastAPI
- [ ] Dashboard : accueil mobile-first avec stats globales + quick links
- [ ] Search : recherche unifiée Meilisearch + deep links
- [ ] Responsive UI : mobile + desktop (Tailwind)
- [ ] Audit basique : logs d'accès (user, timestamp, action)
- [ ] Auto-login : deep links avec tokens vers Immich/Paperless
- [ ] Immich proxy : FastAPI wrapper pour tokens
- [ ] Paperless proxy : FastAPI wrapper pour tokens

### Phase 2 — Enhanced (Semaines 4-6)
- [ ] Filtrage avancé : type, date range, tags
- [ ] ACL granulaire : permissions par utilisateur
- [ ] Partage temporaire : lien avec expiration
- [ ] PWA : offline support + notifications
- [ ] Orchestration : n8n workflows (archivage, OCR, backup)
- [ ] Meilisearch indexing : auto-sync depuis Immich/Paperless

### Phase 3 — Advanced (Semaines 7+)
- [ ] Archivage progressif : politiques de rétention
- [ ] Conversion formats : on-demand via n8n
- [ ] Monitoring : Dashboard Grafana + alertes ntfy
- [ ] Partage collaboratif : groupes d'accès
- [ ] Analytics : statistiques d'usage par utilisateur

---

## API Contracts (FastAPI)

### Auth
```
GET  /api/v1/auth/login    → JWT token + services URLs
POST /api/v1/auth/logout
POST /api/v1/auth/refresh  → nouveau JWT
```

### Search
```
GET /api/v1/search?q=facture&type=document&limit=20
→ { results: [{id, title, type, source, url, snippet, score}], total }
```

### Proxy & Deep Links
```
GET /api/v1/proxy/paperless/documents/{id}/  → forward avec token
GET /api/v1/proxy/immich/assets/{id}/        → forward avec token
```

### Sharing & ACL
```
POST /api/v1/share                    → {token, url, expires_at, permission}
GET  /share/{token}                   → redirect (no auth needed)
GET  /api/v1/audit?user=&action=&limit=
```

### Admin
```
GET  /api/v1/stats          → {total_docs, total_photos, indexed}
POST /api/v1/admin/reindex  → {status, eta_seconds}
```

---

## Data Models

```python
class SharedLink:
    id: str           # Unique token
    source: str       # "paperless" | "immich"
    source_id: str
    title: str        # Cached
    created_by: str   # Tailscale user
    created_at: datetime
    expires_at: datetime
    permission: str   # "read" | "edit"

class AuditLog:
    id: str
    user: str         # Tailscale user
    action: str       # "view", "search", "share", "login", "logout"
    object_type: str  # "document", "photo", "share"
    object_id: str
    source: str       # "paperless" | "immich" | "nas-logo"
    timestamp: datetime
    ip_address: str   # Tailscale IP
    details: dict

class AccessPolicy:
    id: str
    resource: str     # "paperless" | "immich" | "nas-logo"
    user: str
    permission: str   # "read", "write", "admin"
    created_at: datetime

class Session:
    user: str
    jwt_token: str
    refresh_token: str
    expires_at: datetime
    issued_at: datetime
```

---

## Infrastructure

| Composant | Techno |
|---|---|
| Database | PostgreSQL (audit logs, ACL, share links) |
| Cache & Session | Redis (JWT tokens, search cache) |
| Search index | Meilisearch |
| Workflow engine | n8n |
| Monitoring | Prometheus + Grafana |
| Alerts | ntfy |
| Backup | Hetzner Storage Box |
| Deployment | Docker Compose sur NAS |

---

## Success Criteria (MVP)

- SSO Tailscale : login unique, zero friction
- Search unifiée : recherche docs + photos en < 300ms
- Mobile-first UI : responsive iPhone/Android + desktop
- Deep linking : auto-login vers Immich/Paperless
- Audit logs : tous les accès loggés
- Token management : JWT + refresh auto
- Deployment : Docker Compose sur NAS
- API : documentation OpenAPI auto-générée

---

## Out of Scope (Phase 1)

- Multiple utilisateurs collaborant (Phase 2)
- Mobile native apps
- Real-time sync (WebSocket) — polling OK
- Advanced ML (auto-tagging)
- Federated GED (multi-NAS)
- Full GDPR compliance
