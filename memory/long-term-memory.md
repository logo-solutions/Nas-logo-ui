---
updated: never
---

# Mémoire Long Terme — Préférences Confirmées

Ce fichier s'enrichit progressivement. Une entrée n'est ajoutée que si elle apparaît dans au moins deux sessions différentes ou si elle a été explicitement confirmée par l'utilisateur.

## Préférences techniques

- **Conteneurs :** Colima uniquement (jamais Docker Desktop — conflits de ports et credsStore)
- **Ports :** toujours binder sur `0.0.0.0`, jamais `127.0.0.1`
- **Langue :** réponses en français

## Patterns de travail validés

- **Symlinks sous-dossiers uniquement pour ~/Documents** : ne jamais symlinkter ~/Documents en entier — les apps sandboxées Mac App Store peuvent perdre l'accès. Symlinkter seulement les gros sous-dossiers depuis ~/Documents/. Confirmé 2026-04-26.
- **Vérifier les tailles avec `--apparent-size`** : `du -sh` varie selon la granularité des blocs (SSD vs HDD). Toujours confirmer avec le nombre de fichiers (`find | wc -l`) pour valider une copie.

## Anti-patterns à éviter

<!-- Comportements que l'utilisateur a corrigés au moins une fois -->

## Conventions du projet NAS-logo

- Stack : Ansible + Docker Compose sur Mac Mini
- Rôles Ansible dans `roles/`, playbooks à la racine
- Variables sensibles dans `group_vars/all/vault.yml` (chiffrées Ansible Vault)

<!-- Consolidation 2026-04-26 09:40 -->
- Parfait. Plan d'action :\n\n1. **Formater** le 6 To en APFS (un seul volume)\n2. **Migrer** les données du SSD → nouveau disque (rsync, Docker arrêté)\n3. **Mettre à jour** `vars.yml` avec le nouveau mount point\n4. **Tester** et vérifier\n\nVoyons d'abord ce qu'il y a sur le SSD à migrer.C'est un disque USB externe de 6 To, actuellement APFS vide. Dans ce contexte : **non, ZFS ne serait pas utile ici**.\n\n### Pourquoi ZFS ne s'applique pas bien\n\n- **Disque unique** — le vrai apport de ZFS est RAIDZ (intégrité sur pool de plusieurs disques). Sur un seul disque, tu perds 90% des bénéfices.\n- **kext tiers sur macOS** — OpenZFS pour macOS est un kernel extension non-officiel. Dans ce projet tu as déjà subi exactement ça avec `apcupsd` (kext cassé sur macOS récent). Même risque à chaque mise à jour macOS / sécurité Apple Silicon.\n- **Accès portable** — si le Mac Mini meurt et que tu branches ce disque sur un autre Mac, un disque APFS s'ouvre nativement. Un disque ZFS nécessite d'avoir OpenZFS installé.\n\n### APFS est le bon choix ici\n\nAPFS sur macOS couvre déjà les cas d'usage concrets :\n- Checksums sur les métadonnées (cohérence structurelle)\n- Snapshots natifs (utile si tu veux des points de restauration locaux avant le rclone)\n- Copy-on-write\n- Zéro dépendance\n\n### Quand ZFS aurait du sens\n\nSi un jour tu achètes un **NAS dédié multi-disques** (TrueNAS, ou un boîtier 4+ baies sous Linux) — là ZFS avec RAIDZ2 est exactement ce qu'il faut, car tu gères un pool avec redondance et checksums sur les données réelles.\n\n**Recommandation :** garde APFS, mais ajoute le chiffrement FileVault sur ce disque vu qu'il est physiquement mobile et contient 6 To de données familiales. Un `diskutil apfs enableFileVault /Volumes/NAS-LOGO-DATA` suffit.Parfait. Les deux symlinks sont en place et pointent correctement.\n\n**Résultat :**\n- `~/Documents/abc` → `/Volumes/NAS-LOGO-DATA/Documents/abc` (37 Go libérés sur SSD)\n- `~/Documents/SSD` → `/Volumes/NAS-LOGO-DATA/Documents/SSD` (9,1 Go libérés)\n- **~46 Go libérés sur ton SSD**, tous les fichiers accessibles au même chemin qu'avant\n\nLes apps non-sandboxées voient exactement le même chemin. `~/Documents` lui-même reste local — aucun risque de compatibilité.Pour un Mac Mini sous macOS avec Colima, voici le comparatif :\n\n## APFS vs ext4 vs ZFS pour ton NAS\n\n| | APFS | ext4 | ZFS |\n|---|---|---|---|\n| Support macOS | Natif | Pilote tiers (Paragon) | OpenZFS (Homebrew) |\n| Performance SSD | Excellent (optimisé SSD) | Bon | Bon |\n| Snapshots | Oui (natif) | Non | Oui (excellent) |\n| Intégrité données | Basique | Basique | Checksums + self-healing |\n| Chiffrement | FileVault natif | Non | Oui |\n| Complexité | Minimale | Moyenne (driver) | Élevée |\n| Docker/Colima | Parfait (bind mounts natifs) | OK avec driver | OK avec driver |\n\n---\n\n## Recommandation\n\n**→ APFS**, sans hésiter pour ton cas.\n\n- C'est macOS natif — aucun driver tiers, aucune friction avec Colima\n- Optimisé SSD (le tien est un SSD externe)\n- Snapshots natifs exploitables avec `tmutil` ou Ansible\n- FileVault peut chiffrer le volume directement\n\n**ext4** n'a aucun sens ici : il faudrait un driver Paragon payant, et le gain est nul sur macOS.\n\n**ZFS** via OpenZFS (`brew install openzfs`) vaut le coup *uniquement* si tu veux des checksums de données et des snapshots ZFS avancés (rollback, send/receive vers Hetzner). La complexité est réelle : kernel extension à gérer, compatibilité macOS updates fragile.\n\n---\n\n## Reformater en APFS (si besoin)\n\n```bash\n# Identifier le disque\ndiskutil list\n\n# Reformater (DÉTRUIT toutes les données !)\ndiskutil eraseDisk APFS \ — confirmé 2026-04-26 09:40

<!-- Consolidation 2026-04-26 16:04 -->
- Parfait. Plan d'action :\n\n1. **Formater** le 6 To en APFS (un seul volume)\n2. **Migrer** les données du SSD → nouveau disque (rsync, Docker arrêté)\n3. **Mettre à jour** `vars.yml` avec le nouveau mount point\n4. **Tester** et vérifier\n\nVoyons d'abord ce qu'il y a sur le SSD à migrer.C'est un disque USB externe de 6 To, actuellement APFS vide. Dans ce contexte : **non, ZFS ne serait pas utile ici**.\n\n### Pourquoi ZFS ne s'applique pas bien\n\n- **Disque unique** — le vrai apport de ZFS est RAIDZ (intégrité sur pool de plusieurs disques). Sur un seul disque, tu perds 90% des bénéfices.\n- **kext tiers sur macOS** — OpenZFS pour macOS est un kernel extension non-officiel. Dans ce projet tu as déjà subi exactement ça avec `apcupsd` (kext cassé sur macOS récent). Même risque à chaque mise à jour macOS / sécurité Apple Silicon.\n- **Accès portable** — si le Mac Mini meurt et que tu branches ce disque sur un autre Mac, un disque APFS s'ouvre nativement. Un disque ZFS nécessite d'avoir OpenZFS installé.\n\n### APFS est le bon choix ici\n\nAPFS sur macOS couvre déjà les cas d'usage concrets :\n- Checksums sur les métadonnées (cohérence structurelle)\n- Snapshots natifs (utile si tu veux des points de restauration locaux avant le rclone)\n- Copy-on-write\n- Zéro dépendance\n\n### Quand ZFS aurait du sens\n\nSi un jour tu achètes un **NAS dédié multi-disques** (TrueNAS, ou un boîtier 4+ baies sous Linux) — là ZFS avec RAIDZ2 est exactement ce qu'il faut, car tu gères un pool avec redondance et checksums sur les données réelles.\n\n**Recommandation :** garde APFS, mais ajoute le chiffrement FileVault sur ce disque vu qu'il est physiquement mobile et contient 6 To de données familiales. Un `diskutil apfs enableFileVault /Volumes/NAS-LOGO-DATA` suffit.Parfait — ça simplifie tout. Pas de rsync, on repart de zéro sur le HDD, et ça valide le setup Ansible bout-en-bout.\n\n**Plan révisé :**\n\n1. Mettre à jour `vars.yml` + les rôles (`personnes`, `n8n`, `stockage`, `monitoring`, `sauvegarde`)\n2. Sur le NAS : arrêter les conteneurs, supprimer les anciens dossiers SSD, `make install`\n\nJe commence l'implémentation ?Parfait. Les deux symlinks sont en place et pointent correctement.\n\n**Résultat :**\n- `~/Documents/abc` → `/Volumes/NAS-LOGO-DATA/Documents/abc` (37 Go libérés sur SSD)\n- `~/Documents/SSD` → `/Volumes/NAS-LOGO-DATA/Documents/SSD` (9,1 Go libérés)\n- **~46 Go libérés sur ton SSD**, tous les fichiers accessibles au même chemin qu'avant\n\nLes apps non-sandboxées voient exactement le même chemin. `~/Documents` lui-même reste local — aucun risque de compatibilité.Pour un Mac Mini sous macOS avec Colima, voici le comparatif :\n\n## APFS vs ext4 vs ZFS pour ton NAS\n\n| | APFS | ext4 | ZFS |\n|---|---|---|---|\n| Support macOS | Natif | Pilote tiers (Paragon) | OpenZFS (Homebrew) |\n| Performance SSD | Excellent (optimisé SSD) | Bon | Bon |\n| Snapshots | Oui (natif) | Non | Oui (excellent) |\n| Intégrité données | Basique | Basique | Checksums + self-healing |\n| Chiffrement | FileVault natif | Non | Oui |\n| Complexité | Minimale | Moyenne (driver) | Élevée |\n| Docker/Colima | Parfait (bind mounts natifs) | OK avec driver | OK avec driver |\n\n---\n\n## Recommandation\n\n**→ APFS**, sans hésiter pour ton cas.\n\n- C'est macOS natif — aucun driver tiers, aucune friction avec Colima\n- Optimisé SSD (le tien est un SSD externe)\n- Snapshots natifs exploitables avec `tmutil` ou Ansible\n- FileVault peut chiffrer le volume directement\n\n**ext4** n'a aucun sens ici : il faudrait un driver Paragon payant, et le gain est nul sur macOS.\n\n**ZFS** via OpenZFS (`brew install openzfs`) vaut le coup *uniquement* si tu veux des checksums de données et des snapshots ZFS avancés (rollback, send/receive vers Hetzner). La complexité est réelle : kernel extension à gérer, compatibilité macOS updates fragile.\n\n---\n\n## Reformater en APFS (si besoin)\n\n```bash\n# Identifier le disque\ndiskutil list\n\n# Reformater (DÉTRUIT toutes les données !)\ndiskutil eraseDisk APFS \ — confirmé 2026-04-26 16:04

<!-- Consolidation 2026-04-26 20:32 -->
- C'est un disque USB externe de 6 To, actuellement APFS vide. Dans ce contexte : **non, ZFS ne serait pas utile ici**.\n\n### Pourquoi ZFS ne s'applique pas bien\n\n- **Disque unique** — le vrai apport de ZFS est RAIDZ (intégrité sur pool de plusieurs disques). Sur un seul disque, tu perds 90% des bénéfices.\n- **kext tiers sur macOS** — OpenZFS pour macOS est un kernel extension non-officiel. Dans ce projet tu as déjà subi exactement ça avec `apcupsd` (kext cassé sur macOS récent). Même risque à chaque mise à jour macOS / sécurité Apple Silicon.\n- **Accès portable** — si le Mac Mini meurt et que tu branches ce disque sur un autre Mac, un disque APFS s'ouvre nativement. Un disque ZFS nécessite d'avoir OpenZFS installé.\n\n### APFS est le bon choix ici\n\nAPFS sur macOS couvre déjà les cas d'usage concrets :\n- Checksums sur les métadonnées (cohérence structurelle)\n- Snapshots natifs (utile si tu veux des points de restauration locaux avant le rclone)\n- Copy-on-write\n- Zéro dépendance\n\n### Quand ZFS aurait du sens\n\nSi un jour tu achètes un **NAS dédié multi-disques** (TrueNAS, ou un boîtier 4+ baies sous Linux) — là ZFS avec RAIDZ2 est exactement ce qu'il faut, car tu gères un pool avec redondance et checksums sur les données réelles.\n\n**Recommandation :** garde APFS, mais ajoute le chiffrement FileVault sur ce disque vu qu'il est physiquement mobile et contient 6 To de données familiales. Un `diskutil apfs enableFileVault /Volumes/NAS-LOGO-DATA` suffit.Parfait — ça simplifie tout. Pas de rsync, on repart de zéro sur le HDD, et ça valide le setup Ansible bout-en-bout.\n\n**Plan révisé :**\n\n1. Mettre à jour `vars.yml` + les rôles (`personnes`, `n8n`, `stockage`, `monitoring`, `sauvegarde`)\n2. Sur le NAS : arrêter les conteneurs, supprimer les anciens dossiers SSD, `make install`\n\nJe commence l'implémentation ?Parfait. Les deux symlinks sont en place et pointent correctement.\n\n**Résultat :**\n- `~/Documents/abc` → `/Volumes/NAS-LOGO-DATA/Documents/abc` (37 Go libérés sur SSD)\n- `~/Documents/SSD` → `/Volumes/NAS-LOGO-DATA/Documents/SSD` (9,1 Go libérés)\n- **~46 Go libérés sur ton SSD**, tous les fichiers accessibles au même chemin qu'avant\n\nLes apps non-sandboxées voient exactement le même chemin. `~/Documents` lui-même reste local — aucun risque de compatibilité.Parfait — tous les services ont `restart: unless-stopped`. Dès que Colima démarrera, tous les conteneurs remonteront automatiquement.\n\nJe crée le template et j'update le rôle docker. — confirmé 2026-04-26 20:32
- Non, regardons exactement ce que sont les fichiers vrac dans `~/Documents`. — confirmé 2026-04-26 20:32
- Avant de lancer, aligne-toi avec moi sur quelques points :\n\n**Ce que fait `make install` :** il rejoue les 15 rôles Ansible (base, stockage, acces, securite, docker, immich, personnes, monitoring, sauvegarde, paperless, recherche, smb, gmail, n8n, whisper). C'est idempotent — ça ne recrée pas ce qui existe déjà.\n\n**Questions avant de lancer :**\n\n1. **Documents** — tu dis que tu n'as pas besoin de garder la structure actuelle. Tu veux qu'on nettoie quoi exactement ? Les doublons sur le SSD ? Que `~/Documents` devienne un symlink vers le HDD ?\n\n2. **C'est un re-run ou une réinstall complète ?** Le NAS a des données (Immich, Paperless, n8n) — un re-run Ansible ne les touche pas, mais si tu veux repartir de zéro c'est différent.\n\n3. **Le HDD `/Volumes/NAS-LOGO-DATA`** est-il bien monté et accessible en ce moment ?\n\nDis-moi ce que tu veux faire et je vérifie l'état actuel avant de toucher quoi que ce soit. — confirmé 2026-04-26 20:32
